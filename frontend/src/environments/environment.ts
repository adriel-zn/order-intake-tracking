export const environment = {
  production: true,
  // In production this assumes the API is reachable at /api on the same origin
  // the app is served from (e.g. behind a reverse proxy). Override at build
  // time if the API is hosted elsewhere.
  apiBaseUrl: '/api',
};
